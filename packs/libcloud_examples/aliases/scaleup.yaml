---
name: "scaleup"
description: "Scale up your environment"
action_ref: "multicloud_examples.example_scale_event"
formats:
  - "scaleup {{cloud}} {{domain}} {{number=1}}"
ack:
  append_url: false
  format: "Provisioning new servers to your cluster"
result:
  format: |
    provisioned some new servers for you. Hope your website gets faster.
